<%= form_for(@request_log) do |f| %>
  <% if @request_log.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@request_log.errors.count, "error") %> prohibited this request_log from being saved:</h2>

      <ul>
      <% @request_log.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :raw %><br>
    <%= f.text_area :raw %>
  </div>
  <div class="field">
    <%= f.label :headers %><br>
    <%= f.text_area :headers %>
  </div>
  <div class="field">
    <%= f.label :source %><br>
    <%= f.text_field :source %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
